apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fbapp.fullname" . }}-test-connection"
  labels:
    {{- include "fbapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fbapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
